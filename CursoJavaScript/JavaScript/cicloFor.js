
const nombres = ['carlos', 'brayan', 'mario', 'alfredo', 'raul', 'paola'];
nombres.forEach((nombre) => {
    console.log(nombre);
});

/**
 * esta variable almacena el nombre del usuario
 * @type {String}
 */
const nombres2 = 'carlos';

/**
 * esto es un objeto
 * @type {object}
 */
const numeros =  {
    id: 1,
    nombre: 'carlos',
    edad: 30
}